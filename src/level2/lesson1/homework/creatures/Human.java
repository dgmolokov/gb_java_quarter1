package level2.lesson1.homework.creatures;

public class Human extends Competitor implements CompetitorInterface {
  public Human(String name, int maxRunDistance, int maxJumpDistance) {
    super(name, maxRunDistance, maxJumpDistance);
  }
}
